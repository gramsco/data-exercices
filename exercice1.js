/* Travail sur les arbres remarquables de Paris */
const data = require("./arbresremarquablesparis.json");

// Combien d'arbres remarquables à Paris ?
const nbOfTrees = null; // number

// Combien d'espèces y a-t-il parmis les arbres remarquables ?
const nbOfSpecies = null; // number

// Dans combien d'arrondissements trouve-t-on des arbres remarquables ?
const nbOfArrondissements = null; // number

// Quel arrondissement compte le plus d'arbres remarquables ?
const arrdt = null; // string

// Quand a été planté le premier arbre remarquable ?
const oldestPlantedTree = null; // date

// Ecris une fonction qui permet de filtrer les données en fonction d'une clé (ex: arrondissement) et d'une valeur (ex:PARIS 17E ARRDT)

// 1) si aucune clé n'est mentionnée (keys.length === 0), doit vérifier tous les champs
// 2) si aucune valeur n'est mentionnée, doit retourner tout le tableau
// 3) sinon retourner null

function queryData(data, value, keys = []) {}

module.exports = {
  /* exporte ici ce dont tu auras besoin pour l'exercice 2 */
};
